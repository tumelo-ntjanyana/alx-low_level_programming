#include "main.h"
#include <stdio.h>

/**
 * _isupper - function block
 * Description: check for uppercase
 * @c: integer
 * Return: 1 if c is uppercase, 0 otherwise
 */

int _isupper(int c)
{
	if (x >= 65 && x <= 90)
	{
		return (1);
	}
		return (0);
}
