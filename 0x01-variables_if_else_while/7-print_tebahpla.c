#include <stdio.h>
/**
 * main - Print the alphabet in lowercase letters in reverse
 *
 *
 * Return: Always 0 (Success)
 *
 */

int main(void)
{
	char a;

	for (a = 'z'; a >= 'a'; a--)
	{
		putchar(letter);
	}
		putchar('\n');
		return (0);
}
